# sql-study
Estudo prático de SQL puro (PostgreSQL) — JOINs, GROUP BY, HAVING e subqueries — usando o schema real do meu sistema em produção (Nippon Detail &amp; Custom), como complemento ao uso diário de SQLAlchemy.
